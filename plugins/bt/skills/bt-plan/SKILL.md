---
name: bt:plan
description: Scaffold only. Future HOW-phase planning skill that turns requirements into an implementation spec and execution plan.
---

# bt:plan

This skill is intentionally stubbed.

## Intended contract
- Own the `HOW to build it` phase after `bt:btrainstorm`.
- Produce an implementation spec and execution plan from a requirements doc.
- Optionally create Beads later, once that workflow is designed on purpose instead of bolted on.

## Status
- Not implemented yet.
- This stub exists so the plugin contract is explicit before behavior is written.
